name: worker_app
description: WorkFlow Worker Mobile App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.7

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  supabase_flutter: ^2.9.0
  flutter_dotenv: ^5.2.1
  geolocator: ^13.0.2
  permission_handler: ^11.4.0
  intl: ^0.20.2
  google_fonts: ^6.1.0
  shared_preferences: ^2.3.4
  url_launcher: ^6.2.5
  http: ^1.2.1
  core:
    path: ../../packages/core
  supabase_client:
    path: ../../packages/supabase_client
  ui_components:
    path: ../../packages/ui_components

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - .env
