import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Smoke test', (WidgetTester tester) async {
    // Worker App requires Supabase initialization; basic smoke test placeholder
    expect(1 + 1, 2);
  });
}
