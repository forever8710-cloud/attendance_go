# Claude CODE CLI 수정사항

**날짜**: 2026/02/06
**버전**: v1.4
**대상**: 관리자 웹페이지 - 계정 관리, 근로자 관리 레이아웃, 설정 UI

---

## 1. 사이드바에 "계정 관리" 메뉴 추가

- [x] `side_nav_drawer.dart` — NavigationRailDestination 추가 (index 5, Icons.manage_accounts)
- [x] `main.dart` — AccountsScreen import + `_buildPage()` switch에 index 5 추가

---

## 2. 계정 관리 화면 (신규)

- [x] `lib/features/accounts/providers/accounts_provider.dart` — AccountRow 모델, AccountsRepository, Riverpod providers
- [x] `lib/features/accounts/presentation/accounts_screen.dart` — 마스터-디테일 레이아웃
  - 좌측 패널 (320px): 계정 목록 + 검색, 역할 배지(근로자/관리자)
  - 우측 패널: 계정 상세/생성 폼 (이름, 전화번호, 이메일, 역할, 상태)
  - 활성/비활성 토글 기능

---

## 3. 근로자 관리 — 마스터-디테일 레이아웃 변경

- [x] `workers_screen.dart` — 세로 배치(폼 상단 + 리스트 하단) → 가로 배치(좌측 목록 + 우측 폼)
  - 좌측 패널 (320px): 근로자 리스트 (이름, 직무·사업장, 상태칩) + 검색 + 신규 등록 버튼
  - 우측 패널: HR카드 폼 (ConstrainedBox maxWidth: 800, 좌측 정렬)
  - 폼 compact화: fontSize 13/12, contentPadding 10px, spacing 12px

---

## 4. 인사기록카드에 "ID", "사번" 필드 추가

- [x] `workers_repository.dart` — WorkerRow에 `employeeId` (String?) 필드 추가, copyWith 갱신
- [x] 데모 데이터에 EMP-001~006 사번 추가
- [x] `workers_screen.dart` — 1행: ID(읽기전용, grey 배경) | 사번 | 이름* | 주민번호 | 성별

---

## 5. 설정 화면 — 좌측 정렬 + 최대 너비 제한

- [x] `settings_screen.dart` — Card를 `Align(alignment: Alignment.centerLeft)` + `ConstrainedBox(maxWidth: 800)` 으로 감싸기

---

## 수정 파일 목록

| 파일 | 변경 내용 |
|------|----------|
| `lib/core/widgets/side_nav_drawer.dart` | 계정 관리 메뉴 추가 |
| `lib/main.dart` | AccountsScreen import + switch 추가 |
| `lib/features/workers/presentation/workers_screen.dart` | 마스터-디테일 레이아웃, ID/사번 추가, 폼 compact화 |
| `lib/features/workers/data/workers_repository.dart` | WorkerRow에 employeeId 추가 |
| `lib/features/settings/presentation/settings_screen.dart` | maxWidth 제한, 좌측 정렬 |
| `lib/features/accounts/presentation/accounts_screen.dart` | 신규 — 계정 관리 화면 |
| `lib/features/accounts/providers/accounts_provider.dart` | 신규 — 계정 모델/리포지토리/프로바이더 |

---

## 작업 상태

| 항목 | 상태 |
|------|------|
| 사이드바 계정 관리 메뉴 | ✅ 완료 |
| 계정 관리 화면 | ✅ 완료 |
| 근로자 마스터-디테일 레이아웃 | ✅ 완료 |
| ID/사번 필드 추가 | ✅ 완료 |
| 설정 좌측 정렬 + maxWidth | ✅ 완료 |
| flutter analyze | ✅ No issues found |
